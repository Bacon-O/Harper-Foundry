# Custom Compile Scripts

Add custom compile script variants here. They override official scripts with the same name.

**Usage:**
```bash
./start_build.sh --exec myconfig.sh
```

See [Official compile scripts](../../compile_scripts/README.md) for reference implementations.
